---
"@stll/folio-core": patch
---

Keep a `w:keepNext` paragraph on the same page as the start of the table that follows it: the table's header rows and its first row, or only that row's first line when the row may break across pages.
