---
"@stll/folio-core": minor
---

Add canonical text segments and paragraph formatting to split and merge content comparison events. Document-version comparison now shares one bounded comparison session across all package stories and reports aggregate resource-limit failures through `FolioVersionComparisonLimitError`.
