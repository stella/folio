---
"@stll/folio-core": patch
---

Honor the case of `\* roman`/`\* ROMAN` and `\* alphabetic`/`\* ALPHABETIC` field format switches, and support the `\#` numeric picture switch for page/sequence number fields.
