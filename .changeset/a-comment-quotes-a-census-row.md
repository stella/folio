---
"@stll/folio-core": patch
---

Quote the census row a schema comment cites in the spelling the census now uses: a path segment names the kind it steps into, so the row reads `content[paragraph].content[run].formatting`.
