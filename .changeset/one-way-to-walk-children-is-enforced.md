---
"@stll/folio-core": patch
---

Record `Comment.preserved` in the reserved-value registry, whose field-level totality gate caught it, and note on `serializeDocumentToDocx` that it writes a built document rather than a parsed one.
