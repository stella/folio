---
"@stll/folio-core": patch
---

Keep a `w:fldChar` whose complex field the paragraph never closes, along with the run and the text around it, and preserve the `w:ffData` a legacy form field hangs on it.
