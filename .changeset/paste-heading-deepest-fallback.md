---
"@stll/folio-core": patch
---

A heading pasted at a level the open document defines no style for lands on the deepest heading style it does define, instead of keeping a style id that resolves to nothing.
