---
"@stll/folio-core": patch
---

Insert a blank page before a `nextPage` section whose `w:pgNumType w:start` restart would give two consecutive pages both odd or both even page numbers while `w:evenAndOddHeaders` is on, so odd and even page numbers keep alternating.
