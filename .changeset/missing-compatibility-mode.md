---
"@stll/folio-core": patch
---

Lay out justified text in a package without `w:compat/w:compatSetting[@w:name="compatibilityMode"]` with the legacy fitting rules, and declare `compatibilityMode` 15 on documents created from scratch.
