---
"@stll/folio-core": patch
---

A numbered paragraph whose direct `w:ind` states `w:firstLine="0"` or `w:hanging="0"` no longer keeps the numbering level's hanging indent, so its marker starts at the paragraph's left edge.
