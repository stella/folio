---
"@stll/folio-vue": patch
---

The editor ref API's AI-edit snapshot is built with the document's style table, so a heading whose style id is localized reaches the agent as a heading. The outline sidebar reads the same table.
