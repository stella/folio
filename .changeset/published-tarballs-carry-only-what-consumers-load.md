---
"@stll/folio-core": patch
"@stll/folio-react": patch
---

Publish only the files consumers load: `dist`, the `src` the `exports` map resolves to, and the licence and notice texts. Tests, snapshots, fixtures, build scripts and `tsconfig` files no longer ship.
