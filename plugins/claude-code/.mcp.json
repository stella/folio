{
  "mcpServers": {
    "folio": {
      "command": "npx",
      "args": ["-y", "@stll/folio-cli@0.2.0", "mcp", "--root", "${CLAUDE_PROJECT_DIR}"],
      "env": {
        "FOLIO_AUTHOR": "${user_config.author}"
      }
    }
  }
}
