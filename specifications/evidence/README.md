# Behavior evidence

Each record connects a compatibility or schema claim to pinned sources and a
reproducible fixture. Application-specific records must include the relevant
application and version scope; records without an application scope describe
format-level requirements.

Records are data, not prose documentation. Keep claims narrow enough for one
fixture assertion to confirm or reject them.
