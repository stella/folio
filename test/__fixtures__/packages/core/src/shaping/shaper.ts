// A shaping implementation in TypeScript beside the boundary: two answers to
// one question is exactly what the single Rust implementation prevents.
import { parseOpenTypeLayout } from "some-shaping-library";

export const shape = parseOpenTypeLayout;
